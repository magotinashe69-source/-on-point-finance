# On Point Finance — Python dependencies (pinned to tested versions).
# Install with:  pip install -r requirements.txt

# Core web app
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-Login==0.6.3
Flask-WTF==1.3.0
Flask-Limiter==4.1.1
python-dotenv==1.2.2

# PostgreSQL driver (used when DATABASE_URL points to Postgres, e.g. Neon).
# psycopg 3 ships prebuilt wheels (pip-only, no system libs needed on Windows).
psycopg[binary]==3.3.4

# Reports (used from Phase 6)
reportlab==4.4.10
openpyxl==3.1.5

# Production server (used from Phase 9)
waitress==3.0.2

# Testing
pytest==9.1.1
